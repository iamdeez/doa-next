---
작성: Planning Agent (갱신: Security Agent → Docs Agent)
버전: v1.3
최종 수정: 2026-07-01 14:25
상태: GAP-013-08 해결됨(SEC-001 Development 수정·65/65 PASS). GAP-013-03/04 Retrospective 위임. GAP-013-09~11 Medium Retrospective 위임.
---

# Gaps: 013-flutter-customer-phase2

> 기획·설계 공백 추적. 유형: 기술-결정-필요 / 입력-결함 / 보안-검토-필요 / 검증-defer 등.

| GAP-ID | 유형 | 내용 | 발견 단계 | 처리 방향 | 상태 |
|---|---|---|---|---|---|
| GAP-013-01 | 입력-결함(경미) | spec run-001-spec-agent.md 의 코드 분석이 "CategoryScreen 하드코딩 10개·GET /categories 미호출" 로 기재되었으나, 실제 `mobile/customer_app/lib/features/category/category_screen.dart` 는 이미 `categoriesProvider` 로 GET /categories 호출·렌더링·하드코딩 부재. FR-001/SC-002 는 사실상 充足 상태이며 실제 신규 작업은 FR-002/SC-003(재시도 버튼)·SC-025(P95) 에 한정됨. spec 요구 의도는 유효하여 spec 재작성 불요 — plan.md 가 실제 코드 상태 기준으로 정정 반영함. | 2단계 Planning | plan.md §핵심 설계 FR-001/002 에 실코드 상태 명시. Development 는 재시도 버튼만 신규. | 기록완료(plan 반영) |
| GAP-013-02 | 보안-검토-필요 | SC-016(미가입 이메일 forgot-password 오류 응답)·SC-022/023(전화번호→이메일 find-email)·find-email 자체가 **user enumeration / PII 노출** 표면을 형성한다. spec 은 명시적으로 오류 응답·마스킹 이메일 반환을 요구(FR-015/016, NFR-004)하나, 이는 (a)이메일 가입 여부 노출, (b)전화번호로 가입 이메일(마스킹) 조회 가능이라는 보안 trade-off 를 동반한다. plan 은 spec 을 따르되(마스킹·rate-limit·anonymous), enumeration 완화(일반화된 메시지·rate-limit·로깅) 가능 여부를 Security Agent 가 평가하도록 위임한다. | 2단계 Planning | Security Agent(활성) 검토 위임. plan.md ADR-006·§기타 고려사항·인터페이스 계약에 trade-off 명시. | Security Agent 위임 |
| GAP-013-03 | 문서-갱신-필요 | 신규 운영 의존성 — SMTP 이메일 provider + 환경변수(`SMTP_HOST`·`SMTP_PORT`·`SMTP_USER`·`SMTP_PASS`·`MAIL_FROM`)·Fly secret 이 infra.md §7 배포 체크리스트·§8 알려진 인프라 제약에 미기재. OTP 임계값(`OTP_TTL_MIN=10`분·`OTP_RESEND_WINDOW_SEC=60`초·**`OTP_MAX_ATTEMPTS=5`(SEC-001 추가)**)도 §8 운영 임계값 미기재. `password_reset_otps` 신규 테이블 마이그레이션(`20260701022235_add_password_reset_otps`) 및 **`20260701140100_add_otp_attempts`(SEC-001 attempts 컬럼 추가)** 도 §3 반영 필요. **코드 검증**: `auth.constants.ts` — OTP_TTL_MIN=10, OTP_RESEND_WINDOW_SEC=60, OTP_LENGTH=6, OTP_MAX_ATTEMPTS=5(SEC-001 신규) 확인. `.env.example` SMTP 항목 추가 확인. migration 2종 존재 확인. PATCH-A09 적용(신규 임계값 도입). | 3단계 Design → Docs 코드 검증 추가 → Docs 복귀(SEC-001) OTP_MAX_ATTEMPTS·마이그레이션 갱신 | Retrospective Agent: infra.md §3(마이그레이션 2종)·§7(SMTP 체크리스트)·§8(SMTP 제약·OTP 임계값 3종: TTL/RESEND/MAX_ATTEMPTS) additive 갱신. | 미해결(Retrospective 위임) |
| GAP-013-04 | 문서-갱신-필요 | context.md §2 핵심 모듈(auth) 역할에 "비밀번호 재설정 OTP(POST /auth/forgot-password·/auth/reset-password)·이메일 찾기(POST /auth/find-email)" 미반영, §4 데이터 모델(users 스키마)에 `password_reset_otps` 미등재, MailerPort(`src/infrastructure/mail/`) 신규 어댑터 미기재. 모두 additive — 기존 정의 부정합 아님. **코드 검증**: `auth.controller.ts` — forgotPassword/resetPassword/findEmail 엔드포인트 3종 확인. `auth.service.ts` L219 — OTP expiresAt 설정 확인. `src/infrastructure/mail/` — mail.module.ts·mailer.port.ts·smtp.mailer.ts·stub.mailer.ts 4종 확인. context.md §2 `auth` 행 "비밀번호 재설정/세션" → "비밀번호 재설정 OTP·이메일 찾기/세션" 갱신 권고. PATCH-A10 적용. | 3단계 Design → Docs 코드 검증 추가(PROC-002) | Retrospective Agent: context.md §2(auth 행·MailerPort 추가)·§4(password_reset_otps 추가) additive 갱신. | 미해결(Retrospective 위임) |

| GAP-013-05 | 구현-결함 | SC-017: `auth.repository.ts:50` `revokeAllRefreshTokensByUser` 에서 `this.prisma.tx.refreshToken.updateMany` 사용. `runInTransaction` 외부 호출 시 `tx.refreshToken`이 `undefined` → TypeError (HTTP 500). 단위 테스트에서 해당 메서드가 mock 대체되어 결함 은폐됨. 수정: `this.prisma.tx.refreshToken` → `this.prisma.refreshToken` (비트랜잭션 직접 접근). | 5b 테스트 EXECUTION | Development Agent 복귀: `auth.repository.ts:50` 한 줄 수정 완료. coverage.md SC-017 PASS(test_reset_valid_otp_changes_password) + e2e test_reset_valid_otp_changes_password PASS 확인. | **해결됨** (Dev 복귀 2회차 + 5b 재검증 2회차 PASS) |
| GAP-013-06 | 구현-결함 | SC-010: `faq_screen.dart`가 "FAQ 내용은 추후 제공됩니다." 정적 placeholder 0건 표시. spec SC-010 요구: "정적 FAQ 항목이 1개 이상 표시". 구현이 spec 미충족. | 5b 테스트 EXECUTION | Development Agent 복귀: FAQ 화면에 정적 항목 ≥1개 추가 완료. coverage.md SC-010 PASS(test_faq_has_items + test_faq_renders_without_exception) 확인. | **해결됨** (Dev 복귀 2회차 + 5b 재검증 2회차 PASS) |
| GAP-013-07 | 구현-결함 | SC-011: `notice_screen.dart`가 "등록된 공지사항이 없습니다." 정적 placeholder 0건 표시. spec SC-011 요구: "정적 공지 항목이 1개 이상 표시". 구현이 spec 미충족. | 5b 테스트 EXECUTION | Development Agent 복귀: 공지 화면에 정적 항목 ≥1개 추가 완료. coverage.md SC-011 PASS(test_notice_shows_title + test_notice_renders_without_exception) 확인. | **해결됨** (Dev 복귀 2회차 + 5b 재검증 2회차 PASS) |

| GAP-013-08 | 보안-취약점(High) [SEC-001] | `POST /auth/reset-password` 엔드포인트에 OTP 시도 횟수 rate limit 없음. 6자리 OTP(900K 경우의 수)·10분 만료 조건에서 초당 1000 요청 시 600초(10분) 내 600K 시도 가능 → 브루트포스 공격 가능. NFR-003 rate limit 은 OTP 발급(forgot-password)에만 적용, 검증(reset-password) 미적용. 수정 방향: (1) reset-password 에 per-email OTP 시도 횟수 제한(예: 5회/10분) 추가 또는 (2) `@nestjs/throttler` 글로벌 rate limiter 적용. **Security Agent 코드 수정 권한 없음 → Development Agent 복귀 필요.** | Security Agent | Development 복귀: `password_reset_otps.attempts` 컬럼 추가(schema + migration `20260701140100_add_otp_attempts`), `OTP_MAX_ATTEMPTS=5` 상수(auth.constants.ts), `incrementOtpAttempts` 메서드(auth.repository.ts), `resetPassword` OTP 불일치 시 attempts 증가·5회 도달 시 consumed 처리(auth.service.ts). 회귀 테스트 2건 추가. 단위 290/290 PASS. | **해결됨** (Development 복귀 Security 수정) |
| GAP-013-09 | 보안-권고(Medium) [SEC-002: GAP-013-02 심화] | `POST /auth/forgot-password` 미가입 이메일 404 반환 및 `POST /auth/find-email` 미등록 전화번호 404 반환이 user enumeration 표면을 형성. 이는 spec 요구(SC-016/SC-023, ADR-006)이며 in-email rate limit(60초) 으로 동일 대상 반복 방지. 그러나 IP 수준 글로벌 rate limit 미적용으로 다수 서로 다른 이메일/전화번호 순차 조회 가능. 완화 권고: 글로벌 IP rate limit(`@nestjs/throttler`) 추가. 단, spec 변경 불요(trade-off 수용) — 권고 사항. | Security Agent | Retrospective 위임: spec-level 변경 없이 글로벌 rate limit 추가 검토(014 또는 별도 patch spec) | 미해결(Retrospective 위임) |
| GAP-013-10 | 보안-권고(Medium) [SEC-003] | `resetPassword` 비밀번호 변경(`markOtpConsumed` 트랜잭션) 완료 후 `revokeAllRefreshTokensByUser` 를 별도 best-effort 호출로 처리. 서버 비정상 종료 시 비밀번호는 변경되었으나 기존 세션(refresh token)이 폐기되지 않을 수 있음 — 공격자가 탈취한 기존 refresh token 으로 새 access token 발급 가능. 수정 방향: `revokeAllRefreshTokensByUser` 를 `markOtpConsumed` 트랜잭션 내로 통합 또는 outbox 패턴으로 보장. 단, 발생 가능성이 낮은 엣지케이스이며 access token TTL(15분)로 자연 만료 — Medium 으로 분류. | Security Agent | Retrospective 위임: revoke 원자성 보장 개선 검토(별도 patch spec 권고) | 미해결(Retrospective 위임) |
| GAP-013-11 | 보안-권고(Medium) [SEC-004] | OTP 검증 실패, rate limit 위반(429), find-email PII 접근 이벤트에 대한 보안 감사 로그 미기재. 공격자가 브루트포스·user enumeration 시도 시 추적 불가능. 수정 방향: `@nestjs/common Logger` 로 forgot-password 호출(IP 포함), reset-password 실패(이메일·사유), find-email 조회(이메일 마스킹·IP), 429 이벤트를 WARN 수준으로 기록. 현재 SmtpMailer 는 성공 로그(`Mail sent to ${to}`)만 존재. | Security Agent | Retrospective 위임: 보안 이벤트 로깅 강화 권고(별도 patch spec 또는 014 포함) | 미해결(Retrospective 위임) |

> GAP-013-02 는 spec 충돌이 아니라 spec 충족 시 발생하는 보안 표면이다. spec 변경 없이 Security 단계에서 완화 설계를 검토한다. → GAP-013-09 로 심화 기록.
> GAP-013-03/04 는 문서-갱신-필요(additive) — 코드 정합성 결함 아님. Docs Agent 가 코드 검증 추가(PROC-002/PATCH-A09/A10) 후 Retrospective Agent 위임.
> GAP-013-05/06/07 는 5b EXECUTION 에서 발견된 구현 결함 → Development Agent 복귀 2회차 + 5b 재검증 2회차 PASS로 **해결됨**. Docs Agent 확인 기준: coverage.md 전체 PASS.
> GAP-013-08 은 High 보안 취약점 — Security Agent BLOCKED 판정. Development Agent 복귀 후 수정 필요.
</content>
