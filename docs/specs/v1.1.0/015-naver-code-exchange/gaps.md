---
작성: Planning Agent (Test Agent·Development Agent 추가 항목 포함)
버전: v1.5
최종 수정: 2026-07-03 13:47
상태: 진행중
---

# Gaps: 015-naver-code-exchange

## 목차

- [GAP 목록](#gap-목록)

---

## GAP 목록

| GAP-ID | 유형 | 내용 | 발견 단계 | 처리 방향 | 상태 |
|---|---|---|---|---|---|
| GAP-015-01 | 문서-갱신-필요 | infra.md §5(연결 실패 재시도)·§7(배포 전 체크리스트)·§8(알려진 인프라 제약)에 네이버 아웃바운드 엔드포인트(`nid.naver.com/oauth2.0/token` 토큰 교환·`openapi.naver.com/v1/nid/me` 프로필)와 `NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET` fail-closed·활성 provider 3종(카카오·구글·네이버)이 미등재. 014 당시 네이버 미활성으로 등재되지 않았고 신규 토큰 엔드포인트도 미등재. | 2단계 Planning (PROC-009 배포 환경 cross-reference) | Docs/Retrospective 단계에서 infra.md 갱신 위임(014 GAP-014-06 동일 패턴). [NEEDS CLARIFICATION] 대상 아님 — 문서 갱신 항목. | 미해결 |
| GAP-015-02 | 설계-누락 | tasks.md T-C2(`abstract SocialAuthService` 에 `signInWithNaver()` 추가)가 Dart `implements` 시맨틱상 breaking change 임을 §F/PROC-001 범위에서 식별하지 못함 — TypeScript optional parameter(§F 대상: backend `verify`/`login`)와 달리, Dart 추상 메서드 추가는 `implements SocialAuthService` 로 선언된 기존 구현체(014 `social_login_flow_test.dart` 의 `_StubSocialAuthService`)의 컴파일을 깨뜨린다(모든 멤버 재구현 강제 — 추상 클래스의 기본 구현 유무 무관). | 4단계 Development (T-C2 구현 중 컴파일 오류로 발견) | `_StubSocialAuthService` 에 `signInWithNaver()` override 를 최소 추가(기존 SC 단언·로직 무변경, 카카오와 동일 취소/반환 패턴)하여 컴파일 회복 — Breaking 잔여 참조 처리(03-verification-rules §1-4)로 분류, D 레이어 테스트 로직 변경 아님. 향후 Design 단계 §F 절차에 "Dart abstract 메서드 추가는 언어 수준 breaking change" 체크 항목 추가 권고(Retrospective 위임). | 처리됨(Development) |
| GAP-015-04 | 보안 취약점 (High, 신규 확정) | `[SEC-015-01: Naver 자동연동(Path 3b)의 이메일 소유권 미검증에 의한 계정 탈취]` — code-exchange(client_secret 교환)는 "이 authorization code 가 DOA 앱에 발급되었는가"(앱바인딩)만 보증하며, "naver 프로필의 email 필드를 그 계정 소유자가 실제로 소유하는가"(이메일 소유권)는 별개로 전혀 검증하지 않는다. 공격자가 자신의 정규 naver 계정 프로필 이메일을 victim 의 DOA 가입 이메일로 설정하고 정상 code-exchange 로그인을 완료하면, `SocialAuthService.login()` Path 3b(`social-auth.service.ts:69-101`)가 추가 확인 없이 victim 계정에 자동 연동 + JWT 발급한다. `google.provider.ts` 는 `email_verified==='true'` 명시적 검증이 있으나 `naver.provider.ts`(및 무변경 대조 대상 `kakao.provider.ts`)에는 대응 검증·필드 자체가 없음을 코드 Read 로 확인. spec.md NFR-003 의 "GAP-014-08/10 전제 소거" 주장은 이 벡터(제3자 앱 불필요, 앱바인딩 우회 불필요)에는 적용되지 않는다. | 6단계 선택(Security, SC-018 최종 재감사 — PATCH-014-02 계정해석 3경로 전수분석 중 발견) | main session/사용자 결정(수정 방향 1 채택): naver 를 `AUTO_LINK_PROVIDERS` 에서 제외(`new Set(['kakao','google'])`). naver 로그인 자체(code-exchange)는 유지 — path 3a(재로그인)·path 3c(신규 독립계정)만 허용, path 3b(email 자동연동)는 409 Conflict 로 차단. Development Agent 가 `social-auth.service.ts` 주석 4곳 갱신(SEC-015-01/GAP-015-04 근거 명시) + regression 테스트 신규 추가(`social-auth.service.naver-autolink-exclusion.spec.ts`, SC 비매핑). 5a 단계 Test Agent (AUTHORING) 가 병렬로 SC-006 매핑 테스트(`autolink-policy.spec.ts`·`social-auth.service.naver.spec.ts`)를 naver Out of Scope 재분류로 반전 완료(확인됨). 상세는 `security/security-report.md` SEC-015-01·"PATCH-014-02 — 계정 해석 경로 전수 분석" 절 참조. SEC-015-02(Medium)·SEC-015-03(Low)는 본 조치 범위 밖(별도 후속 — 위 권고 3·4 참조). | 처리됨(Development) — Security 재감사(6단계) 대기 |
| GAP-015-03 | 문서-갱신-필요 | context.md 3개 지점이 naver 재도입으로 부정확해짐(PATCH-A10 §2/§6 기준 해당). **(1) §2 핵심 모듈 목록** `social/`(auth 내부) 행 — "`NaverProvider` 는 파일 보존·미와이어(SEC-001 — 별도 spec 이월)" 서술이 사실과 불일치. 코드 검증: `apps/backend/src/modules/auth/auth.module.ts` L12(import)·L29(providers 배열 등록), `apps/backend/src/modules/auth/social/social-provider.resolver.ts` L4(import)·L18(생성자 주입)·L23(`providers` 맵 `naver: this.naver`) 확인 — `NaverProvider` 가 DI 등록·resolver 매핑 모두 완료되어 활성 상태. 갱신 권고: "`NaverProvider`(code-exchange, `nid.naver.com/oauth2.0/token`→`openapi.naver.com/v1/nid/me`)" 로 교체하고 카카오·구글과 동일하게 "활성" 표기. **(2) §4 데이터 모델** `users.social_accounts`(014) 설명 말미 "활성 provider 카카오·구글(네이버 제외 — SEC-001)" 서술 — 코드 검증(GAP 최초 기록 시점): `apps/backend/src/modules/auth/dto/social-login.dto.ts` L5 `SUPPORTED_PROVIDERS = ['kakao', 'google', 'naver']` 확인 — naver 는 **로그인 provider 로는** 3종 전부 활성. 다만 `AUTO_LINK_PROVIDERS`(email 자동연동 화이트리스트)는 GAP-015-04(SEC-015-01) 조치로 `new Set(['kakao', 'google'])` 로 되돌려져 naver 는 자동연동 대상에서 제외됨(재로그인·신규가입은 계속 가능) — 본 항목(2) 갱신 권고 시 "활성 provider(로그인 가능) 카카오·구글·네이버 3종 vs 자동연동(AUTO_LINK) 허용 카카오·구글 2종(naver 는 이메일 소유권 미검증으로 제외, SEC-015-01)"으로 **두 구분을 분리하여** 서술할 것(단순히 "3종 전부 활성"으로 뭉뚱그리면 부정확). **(3) §6 알려진 제약** "소셜 로그인 아웃바운드 rate limit 부재(SEC-004/GAP-014-06, Low)" 행의 "kapi.kakao.com·oauth2.googleapis.com" 서술에 naver 아웃바운드 2건(`nid.naver.com`·`openapi.naver.com`) 추가 필요(코드 검증: `naver.provider.ts` 의 `fetch()` 호출 2개소 직접 확인). **(4) §7 갱신 이력**(표준 changelog 테이블, PROC-003 "스펙 버저닝 이력" 금지 대상과 무관 — §5 갱신 규칙이 매 spec 완료 시 신규 행 추가를 명시) 015 신규 행 추가 권고. | 6단계 Docs (5b coverage.md·gaps.md 대조 중 발견, PATCH-A10 §2/§6 기준 적용 + PROC-002 코드 검증 수행) | context.md §2·§4·§6·§7 갱신 — Retrospective Agent 위임(Docs Agent 직접 갱신 금지, agent-rules.md §3.1/06-docs.md 핵심원칙 6). | 미해결 |
| GAP-015-05 | 문서-갱신-필요 (spec-text 불일치) | GAP-015-04 결정(naver 를 `AUTO_LINK_PROVIDERS` 에서 제외)으로 spec.md **SC-006**("미연동 네이버 계정의 이메일이 기존 사용자 계정 이메일과 동일할 때 ... 자동 연동되고 JWT 가 반환된다")의 naver 관련 문언이 실제 동작과 더 이상 일치하지 않는다(현재는 409 Conflict 로 거부). **SC-010**("SC-006/007/008 세 경로 모두 ... accessToken·refreshToken 반환")도 동일 사유로 naver 기준 3경로 → 2경로(재로그인·신규가입)로 축소되어야 한다. 5a Test Agent 가 D 레이어 테스트는 실제(차단) 동작에 맞춰 조정 완료했으나(`test-cases.md` v1.1 §SC 매트릭스·§미커버 항목 (4)), **spec.md 원문 자체는 Test Agent 권한 밖**(다른 단계 산출물 수정 금지, agent-rules.md §3.1)이라 미수정 상태로 남아 있다. | 5a Test Agent (SEC-015-01 재작업 중 SC 매핑 재검토 시 발견) | Spec Agent 복귀 시 SC-006 을 "naver 자동연동은 이메일 소유권 미검증 위험(SEC-015-01)으로 제공되지 않으며, 동일 이메일 요청은 409 로 거부된다"로, SC-010 을 "SC-007/008 두 경로"로 갱신 권고. 6단계 Security 재감사(GAP-015-04) 확정 이후 Docs 단계 또는 후속 patch spec 에서 처리해도 무방(코드·테스트는 이미 안전한 상태로 동기화 완료 — 본 GAP 은 문서 정합성 항목). | **처리방향 확정 (2026-07-03, 사용자 결정)**: spec.md 원문은 **미수정 유지**(버저닝 원칙상 완료 spec 은 수정하지 않음, 01-design-rules §2). 최종 정책(naver 자동연동 제외·409 거부)은 context.md §6 + CHANGES.md 후속 주의사항에 명시되어 정합성 확보. 후속 소셜 spec 설계 시 context/CHANGES 를 SoT 로 참조. Spec Agent 복귀는 수행하지 않음. |
</content>
