---
작성: Spec Agent
버전: v1.1
최종 수정: 2026-07-06
상태: 확정
---

# Assumptions: 021-payment-file-integration

| ID | 가정 내용 | 확인 필요 여부 | 확인 방법 |
|---|---|---|---|
| ASM-001 | 본 spec 의 성공 판단 기준은 "이니시스 sandbox 를 통한 실제 charge/refund 승인 성공 + 리턴 데이터 기반 결제/환불 상태 처리 검증 + R2 실 버킷 업로드·공개 URL(r2.dev) 접근 성공"이다. 실 운영(prod MID) 결제 성공은 범위 외. | 아니오(사용자 확정) | — |
| ASM-002 | 결제/파일 실연동의 이해관계자는 기존과 동일(구매자·판매자·플랫폼 운영자)하며 추가 이해관계자(PG 정산 담당·세무 등)는 본 spec 범위에 없다. | 아니오(사용자 확정) | — |
| ASM-003 | PG 가맹점 계약의 사업자 명의는 [TO-VERIFY] — sandbox 단계 진행에는 영향 없으며, 실 MID 발급 시점(범위 외 후속 spec)에 확인한다. | 예 | 실 MID 발급 신청 시 사업자등록 서류로 확인 |
| ASM-004 | 결제 관련 API 응답 P95 ≤2,000ms(NFR-001, 기존 infra.md NFR-002 승계) 기준을 유지하되, 이니시스 sandbox·운영 API 응답 지연은 시스템이 통제할 수 없는 외부 변동 요인으로 인지한다. | 아니오 | — |
| ASM-005 | 본 spec 의 완료 목표 일정은 지정되지 않았다. | 아니오 | — |
| ASM-006 | 이니시스 표준(호스팅) 결제창 방식을 채택한다고 가정 — 카드번호 등 민감 결제정보를 본 시스템이 직접 수집하지 않으므로 PCI-DSS 카드데이터 보관 요구사항은 해당 없음. 채택 결제창 방식이 이와 다르면(비표준/API 직접 카드입력 방식) Design 단계에서 재확인 필요. | 예(결제창 방식 확정 시) | Design Agent 가 이니시스 공식 개발 문서로 결제창 연동 방식 확인 |
| ASM-007 | 초기 예상 트래픽(동시 결제 건수·파일 업로드 빈도)은 미정 — 추후 운영 중 실측한다. 본 spec 의 NFR 은 정성적 목표(P95 기준)로 대체하며 규모 기반 용량 설계는 범위 외. | 아니오 | — |
| ASM-008 | PG/R2 자격증명의 Fly.io secret 등록 등 배포·운영 작업은 사용자 본인이 직접 수행한다. | 아니오 | — |
| ASM-009 | **[정정, GAP-021-01]** PG API 호출 실패(타임아웃·5xx)는 `payment_outbox` 재시도 흐름을 사용하지 않는다 — 해당 outbox 는 성공한 결제완료 이벤트(payment.completed 등)를 pg-boss 로 relay 하는 용도이며 charge 실패 레코드 생성·재시도 메커니즘이 없음(Planning Agent 확인). 대신 실패 시 결제 상태를 `failed` 로 기록하고, 클라이언트가 동일 멱등성 키로 재요청하면 기존 멱등성 계약(P-005)만으로 안전한 재시도가 보장된다. 신규 서버측 재시도 메커니즘은 추가하지 않는다(FR-004). | 아니오(사용자 승인 — 옵션 1) | — |
| ASM-010 | R2(FileStoragePort) 업로드 실패 시 서버는 별도 재시도를 수행하지 않고 클라이언트가 presigned URL 로 직접 재시도할 책임을 진다(기존 presigned URL 모델의 구조적 특성 유지). | 아니오 | — |
| ASM-011 | 결제·파일 데이터의 백업/복구는 기존 Fly Postgres 백업 정책을 그대로 재사용하며 추가 요구사항이 없다. | 아니오 | — |
| ASM-012 | 환불 승인은 기본적으로 자동 처리되며, 관리자 개입이 필요한 예외 케이스가 발생하면 기존 admin 감사로그(admin_audit_logs, 013/018) 패턴을 재사용한다(신규 감사 로그 종류 추가는 본 spec 범위 — FR-006 참조). | 아니오 | — |
| ASM-013 | Cloudflare R2 계정·버킷·API 토큰은 사용자가 Development 단계 진입 직전 직접 준비한다(가입 → R2 활성화 → 버킷 생성 → r2.dev public access 활성화 → API 토큰 발급). 본 spec 은 이 크레덴셜이 Development 착수 시점에 준비되어 있음을 전제로 한다. | 예 | Development Agent 착수 전 사용자가 크레덴셜 준비 완료를 확인 |
