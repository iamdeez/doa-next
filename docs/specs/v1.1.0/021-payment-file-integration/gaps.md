---
작성: Planning Agent — GAP-021-02 RESOLVED 갱신: Test Agent (EXECUTION, 5b) — GAP-021-03/04 코드검증·RESOLVED 갱신: main session(사용자 요청)
버전: v1.6
최종 수정: 2026-07-06 14:00
상태: 완료 (GAP-021-01~04 전건 RESOLVED)
---

# Gaps: 021-payment-file-integration

| GAP-ID | 유형 | 발견 단계 | 내용 | 상태 | 처리 방향 |
|---|---|---|---|---|---|
| GAP-021-01 | 기술-결정-필요 | 2단계 Planning | **SC-007/FR-004/ASM-009 vs payment_outbox 실제 semantics 모순.** 현재 `payment_outbox` 는 charge **성공** 시 `payment.completed` 이벤트를 relay(OutboxRelay 5초 폴링→OrderService.markConfirmed)하는 트랜잭셔널 이벤트 outbox 다(`payment.service.ts` L61-81·`outbox-relay.ts` L44-57). charge **실패**(status=failed 분기)에는 outbox 레코드가 생성되지 않고 charge 재시도 메커니즘도 없다. SC-007("타임아웃/5xx → payment_outbox 에 재시도 대상 레코드 기록")은 (a) 현재 코드에 부재하고 (b) 추가 시 ASM-009("신규 재시도 메커니즘 추가 없음")·P-007(스펙 범위) 위반 → satisfiable 하지 않음. | **RESOLVED** (2026-07-06, 옵션 1) | **해소 완료.** 사용자가 옵션 1 승인 → Spec Agent 가 spec.md v1.2 로 SC-007·FR-004·NFR-002·SC-014 재작성(실패 시 payment=`failed` + 동일 멱등키 재요청 중복방지, payment_outbox 실패기록·신규 재시도 없음) + ASM-009 정정. Planning 재검증 확인 완료(FR-004 L93-96·NFR-002 L119-122·SC-007 L140·SC-014 L147·ASM-009). payment_outbox 실제 semantics(성공 이벤트 relay 전용)와 정합. plan.md ADR-008 확정 반영. |
| GAP-021-02 | 기술-결정-필요 | 3단계 Design | **표준(호스팅) 결제창 채택 시 charge() 의 결제창 인증결과(auth token) 수신 경로 미해소.** ASM-006 분석 결과 표준 결제창 방식이 spec 정합(카드 비수집). 표준결제 2단계 승인 API(서버-투-서버)는 동기 → FR-001 성립. 그러나 승인 API 는 1단계(브라우저 결제창)가 산출한 인증 토큰(authToken/tid `[TO-VERIFY]`)을 요구하는데, 현행 `charge({orderId,amount,idempotencyKey})` 시그니처에는 이를 담을 파라미터가 없다. plan.md "charge 시그니처 무변경" 전제와 상충 가능(refund `pgTransactionId` 확장(ADR-002)과 동형). research.md §4-2 참조. | **RESOLVED** (2026-07-06, 5b 코드 레벨 재확인) | **완전 해소.** Development 1차작업(T003)에서 `charge()`·`refund()` 에 optional 필드(`authToken?`·`pgTransactionId?`) 확장 → PPG-1 동기화 후 사용자 승인(AskUserQuestion) 으로 완전 배선 추가작업 지시 → Development 가 `CreatePaymentDto.authToken?`(optional) 추가 + `PaymentController.pay` 가 `dto.authToken` 을 `PaymentService.pay` 4번째 인자로 전달 + `PaymentService.pay` 가 `gateway.charge({...,authToken})` 로 전달하는 배선을 완결. 5b 가 `create-payment.dto.ts`·`payment.controller.ts`·`payment.service.ts` 3개 파일을 직접 Read 하여 배선 확인, `authToken` 미전달 시 하위호환(undefined 전파, stub·기존 fallback 불변) 확인. unit 409/409·정적 135/135·`payments.e2e-spec.ts` 4건 전 PASS(회귀 0). SC-001~003·013 옵션A 는 이제 실 HTTP API(`POST /payments`) 경로로도 검증 가능(test-cases.md 옵션A caveat 5b 갱신). 잔존: `authUrl`(결제창 동적 승인 URL)은 DTO 에 여전히 미노출 — 이 흐름이 필요한 경우 별도 검토([TO-VERIFY], 차단 아님). |
| GAP-021-03 | 문서-갱신-필요 | 3단계 Design (6단계 Docs 코드검증 갱신) | **context.md·infra.md 실연동 반영 갱신 필요.** context.md §2 file 모듈(L89)·§3.4 외부연동(L166-171)의 "실 R2 연동은 후속"·"REST API(PG사별 SDK)" 표현이 실연동 완료로 부정합. infra.md §3.4 외부연동·§5 연결 실패 재시도(L115-127)·§6 의존성 구조(L165-181)·§7 배포전 체크리스트(L184-197)·§8 알려진 인프라 제약(L200-213) 갱신 필요. | **RESOLVED** (2026-07-06 14:00, main session — 사용자 요청) | Docs Agent 직접 갱신 금지 원칙에 따라 Retrospective 단계로 위임됐던 반영을 main session 이 사용자 요청으로 직접 적용. context.md §2 L89 file 행·L96 인프라 요약·§3.4 외부연동(Cloudflare R2·PG사 행) 갱신 완료 + §7 갱신이력에 021 항목 신규. infra.md §5 연결 실패 재시도(Cloudflare R2 실연동 반영·PG사 행→KG이니시스 구체화+AbortController 타임아웃 명시)·§6 의존성 표(`@aws-sdk/s3-request-presigner` 추가)·§7 체크리스트(`PAYMENT_PROVIDER`/`FILE_STORAGE`·`INICIS_*`·`R2_*` 3개 항목 신규)·§8 알려진 인프라 제약(KG이니시스 실 MID 미발급 신규 행, ASM-003) 갱신 완료. GAP-021-03 권고 내용 전건 반영 확인. |

| GAP-021-04 | 성능-구현-필요 | 선택 단계 Performance | **`IniisisPaymentGateway.charge()`/`refund()` 의 native fetch 호출에 명시적 타임아웃(`AbortController`/`signal`) 부재.** Node 20 내장 fetch(undici) 는 별도 지정 없이는 요청당 기본 5분(`headersTimeout`/`bodyTimeout`) 까지 대기할 수 있어, 이니시스 API 응답 지연 시 해당 결제 요청이 NFR-001 P95(2,000ms) 목표를 크게 벗어나 열려 있을 수 있고, 동시 다발 시 이니시스 host 로의 아웃바운드 커넥션 풀 점유로 후속 결제 요청까지 연쇄 지연될 수 있다. ADR-008/FR-004/SC-007 은 "타임아웃 발생 후" 처리(payment=failed 기록)만 정의했을 뿐, 타임아웃을 실제로 유발하는 상한값 자체는 코드·설계 문서 어디에도 명시되어 있지 않다. `payment.service.ts` 확인 결과 PG 호출이 DB 트랜잭션 외부에서 실행되어 Prisma 커넥션 풀 고갈로는 직결되지 않으나(완화 요인), HTTP 요청-응답 사이클과 아웃바운드 소켓 점유 문제는 남는다. performance-report.md §병목 지점 분석 PERF-BOTTLENECK-001 참조. | **RESOLVED** (2026-07-06 13:46, 코드 배선 완결) | 이전 Development 복귀 시도는 로그상 "단계 복귀" 기록만 남고 실제 코드엔 `FETCH_TIMEOUT_MS` 상수·주석만 추가된 채 fetch 호출에 미배선 상태였다(run-010 산출물 부재 — 세션 중단으로 추정). `charge()`(L128-132)·`refund()`(L183-188) 두 `fetch()` 호출에 `signal: AbortSignal.timeout(FETCH_TIMEOUT_MS)`(10초) 배선을 완결했다. 기존 catch 분기가 `AbortError` 도 일반 `Error` 로 처리하여 `{success:false, failureReason: err.message}` 계약·트랜잭션 경계·멱등성 로직 불변. 검증: `pnpm exec tsc --noEmit` EXIT 0, `inicis-payment-gateway.spec.ts`·`payment.service.spec.ts`(단위) + `inicis-decimal`·`inicis-idempotency`·`payment-outbox-invariant`·`provider-env-switch`·`package-no-aws`(정적) 전 스위트 PASS(회귀 0). |

> **추가 [TO-VERIFY] 항목**(gap 아님 — 정상 위임): 이니시스 API 계약 구체값(파라미터명·서명 알고리즘·엔드포인트·결제수단 코드·공용 테스트 상점ID)은 본 세션 WebFetch 미제공으로 온라인 확인 불가 → plan.md "외부 라이브러리·API 동작 검증" 절에서 전량 [TO-VERIFY] 로 Design/Development 위임(team-lead 승인 범위). ASM-006(결제창 방식)은 Design 최우선 확인 대상.
</content>
