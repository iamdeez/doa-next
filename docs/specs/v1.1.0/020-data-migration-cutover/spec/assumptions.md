---
작성: Spec Agent
버전: v1.0
최종 수정: 2026-07-05
상태: 확정
---

# Assumptions: 020-data-migration-cutover

| ID | 가정 내용 | 확인 필요 여부 | 확인 방법 |
|---|---|---|---|
| ASM-001 | 레거시 18개 서비스의 테이블별 행 수·전체 데이터 용량(GB)은 미상. Planning/Design 단계에서 실측(예: 레거시 RDS 에서 `pg_dump --schema-only`+`SELECT count(*)`, `pg_total_relation_size`) 하여 FR-011 사전평가 리포트에 반영한다. **리스크**: 데이터 규모가 커서 유지보수 윈도우(~1시간, NFR-001) 를 초과할 가능성이 있다 — 초과 시 FR-012 에 따라 사용자 재확인 절차를 거친다. | 예 (Planning/Design 단계 실측 필수) | 레거시 RDS 접속 후 스키마별 행 수·용량 측정 스크립트 실행 |
| ASM-002 | 레거시 18개 서비스 데이터 모델과 신규 8개 스키마(33테이블) 간 매핑은 완전한 1:1 이 아닐 수 있다 — 신규 시스템은 레거시를 그대로 복제한 것이 아니라 처음부터 재설계·재구축되었기 때문이다(예: `variant` 가 옵션을 인라인 필드로 흡수, cross-schema FK 미선언 등 — `context.md §4`). **이는 빅뱅+1시간 윈도우 전략의 최대 리스크**이며, Planning/Design 단계에서 필드 단위 매핑 명세(FR-009)를 최우선 산출물로 선행 작성해야 한다. 매핑 복잡도가 과도하여 윈도우 내 변환이 불가능하다고 판단되면 FR-012 에 따라 윈도우 재산정 또는 부분 사전이관(레거시 스냅샷을 미리 신규 스키마로 변환해두고 컷오버 시점엔 델타만 재변환) 전략을 사용자와 재확인한다. | 예 (Planning/Design 단계 매핑 명세 작성 시 확정) | 레거시 스키마 DDL + `schema.prisma` 대조 분석 |
| ASM-003 | 컷오버 성공 판단 기준: (1) SC-005~SC-007 정합성 검증 100% 통과 AND (2) 트래픽 전환 후 7일간 심각도 High 이상 장애 0건. 7일은 합리적 관찰 기간으로 채택(사용자 미지정). | 아니오 (합리적 기본값, 필요 시 Planning 단계에서 조정 가능) | — |
| ASM-004 | 데이터 정합성 검증 방법은 (1) 테이블별 레코드 수 대조 (2) 금전 필드(주문총액·결제금액·정산금액) 합계 대조 (3) 무작위 샘플 체크섬/해시 비교 3종을 기본 채택(팀리드 제안 반영). | 아니오 | — |
| ASM-005 | 컷오버 유지보수 윈도우는 최소 D-3일 전 고객 앱·판매자 콘솔 공지를 통해 이해관계자(고객·판매자)에게 사전 고지한다. PG사 등 결제 파트너에 대한 별도 공지 필요 여부는 불명 — 낮은 우선순위 가정으로 처리, Planning 단계에서 결제 PG 연동 방식(스텁 상태 — `context.md`) 확인 후 필요 시 추가. | 아니오 (낮은 영향) | — |
| ASM-006 | 빅뱅 전환이므로 도메인 간 "순서" 보다 참조 의존성 위상(topological order) 을 따른다: users → products → commerce → orders → payments → settlements → admin → files. | 아니오 | — |
| ASM-007 | 컷오버 목표 일정(구체적 날짜)은 미지정 — 본 spec 은 일정을 강제하지 않으며, Planning 단계에서 매핑 명세·사전평가 완료 후 목표일을 사용자와 협의한다. | 아니오 (spec 완료를 막지 않음) | Planning 단계에서 협의 |
| ASM-008 | 개인정보(PII)·결제 데이터 이관 채널은 TLS 암호화(NFR-004)를 적용하며, 별도의 추가 규제 요구사항(예: 특정 인증 규격)은 없다고 가정한다. | 아니오 (표준 보안 관행 수준) | — |
| ASM-009 | 이관 작업 실행 환경은 Fly.io 워커/일회성 잡 또는 별도 프로비저닝된 이관 러너(컨테이너/VM) 중 Planning 단계에서 결정한다(HOW 영역이므로 spec.md 범위 외). | 아니오 (plan.md 로 위임) | Planning 단계 결정 |
| ASM-010 | 이관 실행 시점까지 레거시 AWS 자격증명(RDS 접근 등)이 유효하다고 전제한다. | 아니오 (실행 직전 재확인 권장 — 런북 체크리스트 항목으로 반영) | 런북 사전 점검 체크리스트 |
| ASM-011 | 컷오버 실행 승인·실시간 모니터링 주체는 프로젝트 오너(사용자) 본인으로 가정한다(별도 운영팀 부재). | 아니오 | — |
| ASM-012 | 실 파일 바이너리(오브젝트) 이관(레거시 S3 → Cloudflare R2)은 본 spec 범위 외로 확정한다 — `apps/backend/src/modules/file/file.module.ts` 확인 결과 신규 시스템의 `FILE_STORAGE` DI 가 `StubFileStorage`(무네트워크 stub)로 바인딩되어 있어 실 R2 연동 자체가 아직 존재하지 않는다(`context.md` 기 기재 — "실 R2 연동은 후속"). 실 R2 배선 완료가 파일 바이너리 이관의 선행 조건이며, 이는 별도 spec 으로 분리한다. 본 spec 은 `file_assets` 메타데이터(레코드)만 이관 대상에 포함한다(FR-017). | 아니오 (코드 확인으로 확정 — 근거: `file.module.ts` L20 `StubFileStorage` 바인딩) | 코드 확인 완료 (본 spec 작성 시 검증) |
