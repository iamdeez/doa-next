---
작성: Spec Agent (GAP-018-01) / Test Agent AUTHORING (GAP-018-02) / Test Agent EXECUTION (GAP-018-02 판정) / Test Agent AUTHORING [재작업] (GAP-018-02 정정) / Test Agent EXECUTION [재작업] (GAP-018-02 최종 RESOLVED) / Docs Agent (GAP-018-01 코드 재검증, GAP-018-03 신규)
버전: v1.5
최종 수정: 2026-07-04 06:27
상태: GAP-018-01 미해결(Retrospective 위임 예정) · GAP-018-02 RESOLVED · GAP-018-03 미해결(Retrospective 위임 예정)
---

# Gaps: 018-auth-security-hardening

> 기획·설계 공백 추적. 유형: 기술-결정-필요 / 입력-결함 / 보안-검토-필요 / 문서-갱신-필요 등.

| GAP-ID | 유형 | 내용 | 발견 단계 | 처리 방향 | 상태 |
|---|---|---|---|---|---|
| GAP-018-01 | 문서-갱신-필요 | `infra.md` §2(인프라 토폴로지)·§8(알려진 인프라 제약)에 Fly.io 클라이언트 IP 전달 방식(`Fly-Client-IP`/`X-Forwarded-For` 헤더, Express `trust proxy` 설정 필요성)이 미기재. 본 spec(FR-004/NFR-008/SC-008/SC-009)이 이 설정을 도입하므로 infra.md 에 additive 갱신이 필요하다. spec.md "배포 환경 cross-reference 결과 (PROC-009)" 절 참조 — 사용자가 A(spec 범위 포함) 결정 시 함께 위임하기로 확정. | 1단계 Spec (PROC-009 cross-reference 중 발견, 사용자 확정) | Design/Docs Agent: `main.ts` trust proxy 설정 코드 검증 후 Retrospective Agent 위임 — infra.md §2(토폴로지 다이어그램에 프록시 헤더 신뢰 표기)·§8(신규 제약 행: "rate limit IP 식별은 Fly-Client-IP/X-Forwarded-For 신뢰 필요, trust proxy 미설정 시 전역 단일 버킷화") additive 갱신 | 미해결(Retrospective 위임 예정) |
| GAP-018-02 | 입력-결함(구현 gap 여부 판정 완료 — [B] 테스트 오류, 정정 적용 완료) | SC-017(FR-010, best-effort): `AuthService.resetPassword()`(OTP 불일치 분기)·`AuthService.findEmail()` 호출부가 `securityAuditLogger.otpVerificationFailed`/`findEmailAccessed` 를 try/catch 없이 직접 호출한다(`auth.service.ts:262`·`:300`, 5a 시점 확인). `SecurityAuditLogger` 자신은 내부 best-effort(각 메서드 try/catch, throw 안 함)이나, `auth.service.spec.ts` 의 SC-017 wiring 테스트(T020, `SecurityAuditLogger` 를 mock 으로 완전 대체해 강제로 throw)는 이 내부 계약을 우회하므로 call-site 방어가 없으면 RED(`Error` 전파 — `BadRequestException`/정상 응답 대신). 5a AUTHORING 시점 `pnpm --filter backend test` 실행 결과 397건 중 2건 FAIL(`test_SC017_018_reset_password_otp_mismatch_unaffected_by_logger_throw`, `test_SC017_018_find_email_unaffected_by_logger_throw`) — 그 외 회귀 0건. | 5a Test AUTHORING (PPG-1 병렬, 테스트 작성 중 실행 확인) | **5b EXECUTION 판정 완료(2026-07-04)**: **[B] 테스트 오류**. production 은 plan.md 설계("SecurityAuditLogger 전 메서드 try/catch(FR-010)" 단일 책임 집중, ADR-007 이 call-site 중복 방어를 대안에서 명시적으로 배제)와 정확히 일치하며 plan.md 테스트 전략표의 SC-017 Input(`logger.warn throw mock`)으로 검증하면 이미 PASS(`security-audit.logger.spec.ts` 3건). FAIL 하는 `auth.service.spec.ts` wiring 2건은 `SecurityAuditLogger` 전체를 mock 하여 production 에서 도달 불가능한 분기(모든 메서드가 무조건 내부에서 예외를 삼키도록 구현됨)를 전제로 한다 — 테스트 전제 조건 오류. 상세 근거: `test/test-report.md` §GAP-018-02 판정. 권고: 5a Test AUTHORING(또는 `auth.service.spec.ts` 한정 재작업)이 wiring 2건을 제거하거나 plan.md Input 에 맞춰 재작성(real `SecurityAuditLogger` + `PinoLogger.warn` mock 조합). Development 복귀 불요(production 코드 변경 없음). **[재작업] 5a 정정 적용 완료(2026-07-04 06:11)**: 권고안 (b)를 채택 — `auth.service.spec.ts` SC-017 describe 블록을 재작성하여 `SecurityAuditLogger` 전체 mock 대신 실제 `SecurityAuditLogger` 인스턴스 + `PinoLogger.warn` throw mock 조합(plan.md Input 정합)으로 AuthService 를 구성. `pnpm --filter backend typecheck` 0 error, `pnpm --filter backend test` 39 suites/397 tests 전건 PASS 재확인(production 코드 변경 없음, D 레이어 외 변경 0건). `test/test-cases.md`·`test/coverage.md` SC-017 매핑 PASS 로 갱신. | **RESOLVED (5b 재검증 완료, 2026-07-04 06:15)**: 5a 정정을 5b 가 독립적으로 재실행하여 확인 — `pnpm --filter backend typecheck` 0 error, `pnpm --filter backend test` 39 suites/397 tests 전건 PASS(`auth.service.spec.ts` 포함), static 2종 4/4 PASS, e2e rate-limit 6/6 PASS, e2e atomicity 1/1 PASS. `git status` 로 production 코드(`auth.service.ts`·`security-audit.logger.ts`) 무변경 재확인. SC-017·SC-020 전건 PASS 로 전환, gate: PASS 최종 확정. 상세: `test/test-report.md` §GAP-018-02 판정 및 정정 재확인, `test/coverage.md`(v1.2). |

| GAP-018-03 | 문서-갱신-필요 | `context.md §6`(알려진 제약 및 기술 부채) 표의 auth 도메인 4개 행이 본 spec(018)으로 전부 RESOLVED 되었다: (1) "소셜 신규가입 경로 orphan user 위험 (SEC-002/GAP-014-01, Medium)" — `createUser`+`createSocialAccount` 두 INSERT 가 `runInTransaction` 으로 원자화됨(코드 검증: `apps/backend/src/modules/auth/social-auth.service.ts:134-140` `this.prisma.runInTransaction(async () => { const u = await this.repo.createUser(...); await this.repo.createSocialAccount(...); ... })`). (2) "소셜 로그인 아웃바운드 rate limit 부재 (SEC-004/GAP-014-06, Low)" — `social-login`·`naver/state` 에 `@Throttle` 적용됨(코드 검증: `apps/backend/src/modules/auth/auth.controller.ts` L67·L76 `@Throttle({ default: { ttl: THROTTLE_TTL_MS, limit: THROTTLE_SOCIAL_LOGIN_LIMIT } })`·`@Throttle(... THROTTLE_NAVER_STATE_LIMIT)`). (3) "auth reset-password IP rate limit 부재 (SEC-002/GAP-013-09, Medium)" — `forgot-password`·`find-email`·`reset-password` 에 개별 `@Throttle` 적용됨(코드 검증: 동 파일 L106·L114·L123). (4) "resetPassword refresh token revoke 비원자 (SEC-003/GAP-013-10, Medium)" — `revokeAllRefreshTokensByUser` 가 tx-aware 로 전환되고 `resetPassword` 가 단일 `runInTransaction` 으로 통합됨(코드 검증: `apps/backend/src/modules/auth/auth.repository.ts:55` `this.prisma.tx.refreshToken.updateMany(...)`, `apps/backend/src/modules/auth/auth.service.ts:282-288` `await this.prisma.runInTransaction(async () => { await this.authRepository.markOtpConsumed(...); await this.authRepository.revokeAllRefreshTokensByUser(user.id); })`). (5) "auth 보안 감사 로그 부재 (SEC-004/GAP-013-11, Medium)" — `SecurityAuditLogger` 3종 메서드가 실제 호출됨(코드 검증: `auth.service.ts:262` `otpVerificationFailed`, `auth.service.ts:300` `findEmailAccessed`, `apps/backend/src/shared/security/fly-throttler.guard.ts:42` `rateLimitExceeded`). `context.md §1` "현재 버전" 필드와 `§7` 갱신 이력에 018 항목 추가도 함께 필요(git rev-parse --short HEAD 로 커밋 확정 후). | 6단계 Docs (문서-구현 일치성 검증 중 발견) | Retrospective Agent: `context.md §6` 위 4개 행을 삭제 또는 "RESOLVED (018-auth-security-hardening)" 로 전이, `§1`·`§7` 갱신 이력에 018 반영 | 미해결(Retrospective 위임 예정) |

> 013/014 선례와 동일 패턴 — 코드 정합성 결함이 아닌 additive 문서 갱신이므로 Docs Agent 가 코드 검증(PROC-002) 후 Retrospective Agent 에 위임한다.

> **Design Agent 확인 (2026-07-04)**: GAP-018-01 의 코드 도입 지점을 tasks.md 로 확정했다 — `main.ts` `app.set('trust proxy', 1)`(T008, ADR-004) + `resolveClientIp`(Fly-Client-IP→XFF→req.ip, T003). research.md "배포 환경 영향 추정" 절에 infra.md §2/§8 additive 갱신 예정 내용(프록시 헤더 신뢰 표기·"trust proxy 미설정 시 전역 단일 버킷화" 제약)을 명시했다. 본 GAP 은 Design 이 해결하지 않으며(문서-갱신-필요 유형, Design 은 infra.md 직접 수정 금지 — MUST NOT), Docs Agent(6단계) 코드 검증 후 Retrospective Agent 반영. 상태 유지: 미해결(Retrospective 위임 예정). 신규 GAP 발견 0건.

> **Test Agent EXECUTION 확인 (2026-07-04 06:02)**: GAP-018-02 를 [B] 테스트 오류로 판정 완료(근거: `test/test-report.md` §GAP-018-02 판정). production 코드(`auth.service.ts`, `security-audit.logger.ts`)는 plan.md 설계와 일치 확인 — 변경 불요. 파이프라인 관점 gate: FAIL 유지(SC-017·SC-020 미충족) — main session 이 5a 재작업(또는 대상 파일 한정 재작업)으로 라우팅 필요.
>
> **Test Agent EXECUTION [재작업] 재검증 확인 (2026-07-04 06:15)**: 5a [재작업](06:11)이 `auth.service.spec.ts` SC-017 wiring 2건을 plan.md Input 정합 방식(실 `SecurityAuditLogger` 인스턴스 + `PinoLogger.warn` throw mock)으로 재작성 완료 후, 5b 가 전체 스위트를 독립 재실행하여 공식 gate 를 재판정했다 — `typecheck` 0 error, unit **39 suites/397 tests 전건 PASS**, static 2종 4/4 PASS, e2e rate-limit 6/6 PASS, e2e atomicity 1/1 PASS. production 코드 무변경(`git status` 확인, D 레이어 외 변경 0건). GAP-018-02 를 **RESOLVED** 로 최종 판정 — **gate: PASS**. SC-001~020 전건 충족. GAP-018-01 만 미해결(Retrospective 위임 예정)로 잔존, 6단계(Docs Agent) 진행 가능. 상세: `test/test-report.md`(v1.1), `test/coverage.md`(v1.2), `test/coverage-gap.md`(v1.2).
>
> **Docs Agent 확인 (2026-07-04 06:27)**: GAP-018-01 코드 재검증(PROC-002) 완료 — `apps/backend/src/main.ts:11` `app.set('trust proxy', 1)`, `apps/backend/src/shared/security/client-ip.util.ts:7-31` `resolveClientIp()`(Fly-Client-IP→X-Forwarded-For→req.ip 폴백) 실재 확인. `grep -n "trust proxy\|Fly-Client-IP\|X-Forwarded-For" .claude/docs/infra.md` 결과 0건 — 미기재 사실 재확인, additive 갱신 필요 판정 유지(PATCH-A13: 신규 도입 케이스이며 기존 표기 불일치 아님). 상태 변경 없음 — 미해결(Retrospective 위임 예정). **신규 GAP-018-03 등록**: `context.md §6` auth 4개 행이 본 spec 으로 RESOLVED 전환 필요(코드 검증 근거는 GAP-018-03 행 참조) — context.md/infra.md 직접 수정 금지(agent-rules.md §3.1 MUST NOT)이므로 Retrospective Agent 위임. CHANGES.md·DIFF-018 생성 완료(base b3f427d), 문서 일관성 검증(spec.md FR/NFR ↔ 코드 변경 대조) 이상 없음.
